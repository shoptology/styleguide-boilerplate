include ../_includes/modulesData
each module in modulesData
	section(id=module.id, class="shop-module anchor clearfix")
		- var folder = module.order + '_' + module.id
		- var content = '../../modules/' + folder + '/_' + module.id
		!=partial(content, public.modules[folder]._data)